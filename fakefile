all:

strtrim:
	gcc -Wall -Wextra -Werror ft_strlen.c ft_strtrim.c -o strtrim && ./strtrim && rm strtrim
atoi:
	
bzero:

calloc:

isalnum:

isalpha:

isascii:

isdigit:

isprint:

itoa:

memchr:

memcmp:

memcpy:
	gcc -Wall -Wextra -Werror ft_memcpy.c -o memcpy && ./memcpy && rm memcpy
memmove:

memset:

putchar_fd:

putendl_fd:

putnbr_fd:

putstr_fd:

split:

strchr:

strdup:
	gcc -Wall -Wextra -Werror ft_strlen.c ft_strdup.c -o strtrim && ./strtrim && rm strtrim
striteri:

strjoin:
	gcc -Wall -Wextra -Werror ft_strlen.c ft_strjoin.c -o strjoin && ./strjoin && rm strjoin
strlcat:

strlcpy:

strlen:

strmapi:

strncmp:

strnstr:

strrchr:

strtrim:

substr:

tolower:

toupper:
